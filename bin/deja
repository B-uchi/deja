#!/usr/bin/env python3
"""deja launcher — works from anywhere, no install into site-packages needed."""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))

from deja.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
